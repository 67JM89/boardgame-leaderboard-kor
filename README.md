# Boardgame Leaderboard Dashboard

개인/소규모 모임 보드게임 결과를 기록하고, 리더보드·월별 성적·게임별 통계를 확인하기 위한 대시보드입니다.

이 리포지토리는 다음 두 가지를 포함합니다.

- `Board Game Leaderboard.html` : 브라우저에서 여는 보드게임 리더보드 대시보드
- `data/board.xlsx` : 보드게임 플레이 기록(플레이 날짜, 게임 이름, 플레이어, 승점, 감점)

---

## 사용 방법

### 1. 대시보드 열기

1. 이 리포지토리를 로컬 PC에 클론 또는 ZIP 다운로드 합니다.
2. 로컬 폴더에서 `Board Game Leaderboard.html`을 더블클릭하여 브라우저(Chrome 등)로 엽니다.
3. 대시보드는 항상 서버의 최신 데이터를 불러오지만, 화면 상단의 **파일 업로드(XLSX)** 버튼을 사용하여 다른 파일을 업로드하여 활용 할수도 있습니다.
> 이 HTML 파일은 로컬에서만 사용하며, 별도의 서버 없이 브라우저에서 바로 실행됩니다.

---

### 2. 엑셀 데이터 업데이트

1. `data/board.xlsx`를 열어 **새 게임 결과를 입력**합니다.  
   - 플레이 날짜  
   - 게임 이름  
   - 플레이어 이름  
   - 승점 / 감점

2. 저장 후, 리포지토리 루트에서 다음 명령으로 변경사항을 커밋·푸시합니다.

```bash
git add data/board.xlsx
git commit -m "Update board results"
git push origin main
```
---

### 3. 주요 기능

- **전체 리더보드**
  - 현재 포인트, 총 승점, 총 감점, 게임 수, 승리 수, 평균 포인트 표시
  - 정렬 기준:  
    1) 현재 포인트  
    2) 총 승점  
    3) 게임 수  
    4) 총 감점(낮을수록 우위)  
    5) 승리 수

- **게임별 리더보드**
  - 게임 종류별로 같은 정렬 기준으로 순위 표시

- **월별 리더보드**
  - 각 연·월 기간 동안의 결과만을 사용하여 순위 계산

- **플레이 히스토리 & 플레이어 상세 팝업**
  - 날짜별 기록, 포인트 변화, 게임별/월별 누적 통계 확인

---

## 승점 / 감점 규칙

이 리더보드는 각 게임 결과에서 **승점(+)과 감점(-)** 을 합산해 포인트를 계산합니다.

- 각 기록은 아래 컬럼을 가집니다.
  - `승점`: 해당 판에서 얻은 점수 (보통 상위 순위 보상)
  - `감점`: 해당 판에서 잃은 점수 (페널티, 보통 음수 값)

- 한 판에서의 포인트 변화는  
  ```
  포인트 변화 = 승점 + 감점
  ```
  으로 계산합니다.

- 플레이어의 **현재 포인트**는 선택된 기간(필터)에 포함된 모든 게임의 포인트 변화를 순서대로 더한 값이며,  
  0점 아래로 내려가지 않도록 최소 0으로 처리합니다.

---

## 랭킹 정렬 기준과 의미

모든 리더보드는 아래 순서로 랭킹을 결정합니다.

| 순위 | 기준 | 설명 |
|------|------|------|
| 1순위 | **현재 포인트** (높을수록 우위) | 지금까지의 누적 결과를 한눈에 보여주는 최종 점수입니다. |
| 2순위 | **총 승점** (높을수록 우위) | 선택된 기간 동안 획득한 `승점`의 합입니다. 상위 순위(1등, 2등 등)를 자주 할수록 값이 커지며, **게임 실력/성과를 강조**하기 위한 기준입니다. |
| 3순위 | **게임 수** (높을수록 우위) | 해당 기간 동안 참여한 판 수입니다. 많이 참여한 사람을 더 높게 평가하여, "꼴찌를 해도 괜찮으니, 일단 참여를 많이 하자"는 메시지를 반영합니다. |
| 4순위 | **총 감점** (낮을수록 우위) | 선택된 기간 동안의 `감점`(보통 음수)의 절댓값 합입니다. 손실을 적게 낸, 안정적인 플레이를 한 사람에게 유리합니다. |
| 5순위 | **승리** (높을수록 우위) | 1등을 한 횟수입니다. 동일 조건일 때, 더 자주 1등을 한 플레이어가 상위에 오도록 합니다. |

**정렬 기준의 철학:**

- 2순위 **총 승점**은 "상위권 성적을 많이 낸 사람"을 우대하는 기준입니다.  
- 3순위 **게임 수**를 4·5순위보다 앞에 둔 것은  
  **게임 참여도(얼마나 자주 같이 놀았는지)** 를 실력 못지않게 중요하게 본다는 의미입니다.  
- 즉, "꼴찌를 해도 괜찮으니, 게임 참여를 열심히 하세요!"라는 메시지를 담고 있습니다.

---

## 주의 사항

- 이 리포지토리는 **공개(Public)** 로 설정되어 있으므로,  
  `data/board.xlsx`의 플레이어 이름·점수는 외부에서 열람 가능하다는 점을 전제로 사용합니다.
- 리더보드 로직(정렬 기준, 포인트 계산 방식)을 수정할 경우, `Board Game Leaderboard.html`의 JavaScript 코드를 함께 업데이트해야 합니다.

---

## 파일 구조

```
boardgame-leaderboard/
├── Board Game Leaderboard.html          # 리더보드 대시보드 (로컬에서 실행)
├── data/
│   └── board.xlsx      # 게임 기록 데이터 (정기적으로 업데이트)
└── README.md           # 이 문서
```

---

## 라이선스

자유롭게 사용, 수정, 배포 가능합니다.
